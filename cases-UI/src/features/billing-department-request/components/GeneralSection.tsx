/**
 * General section placeholder — Phase 2 skeleton only.
 * Field controls land in Phase 3.
 */
export function GeneralSection() {
  return (
    <section
      className="cases-advanced"
      aria-label="General"
      data-testid="billing-general-section"
    >
      <h2 className="cases-queue__title">General</h2>
      <p className="case-create-placeholder__body" data-testid="billing-general-placeholder">
        General fields will be added in a later phase.
      </p>
    </section>
  )
}
