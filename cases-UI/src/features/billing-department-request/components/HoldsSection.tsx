/**
 * Holds section placeholder — Phase 2 skeleton only.
 * Dual listbox and field controls land in Phase 3.
 */
export function HoldsSection() {
  return (
    <section
      className="cases-advanced"
      aria-label="Holds"
      data-testid="billing-holds-section"
    >
      <h2 className="cases-queue__title">Holds</h2>
      <p className="case-create-placeholder__body" data-testid="billing-holds-placeholder">
        Holds fields will be added in a later phase.
      </p>
    </section>
  )
}
