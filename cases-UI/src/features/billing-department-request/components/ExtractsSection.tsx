/**
 * Extracts section placeholder — Phase 2 skeleton only.
 * Field controls land in Phase 3.
 */
export function ExtractsSection() {
  return (
    <section
      className="cases-advanced"
      aria-label="Extracts"
      data-testid="billing-extracts-section"
    >
      <h2 className="cases-queue__title">Extracts</h2>
      <p className="case-create-placeholder__body" data-testid="billing-extracts-placeholder">
        Extracts fields will be added in a later phase.
      </p>
    </section>
  )
}
