# Feature module template (frontend)

Copy to `src/features/<case-type-kebab>/` and implement:

- `pages/`, `components/`, `hooks/`, `api/`, `types/`, `validation/`
- `routes.tsx`, `index.ts`

Rules: `docs/architecture/06_Feature_Module_Template.md`.

No peer feature imports.
