# Public API base for the Vite app (no secrets).
# Copied to .env for local; used by axios (exrtApi.ts) via import.meta.env.VITE_API_BASE_URL.
# Restart Vite after changes.
VITE_API_BASE_URL=http://localhost:8090
