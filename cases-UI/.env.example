# Public API base for the Vite app (no secrets).
# Used by api/client.ts and exrtApi.ts via import.meta.env.VITE_API_BASE_URL.
# Restart Vite after changes. Backend default port is 8090 (not 8080).
VITE_API_BASE_URL=http://localhost:8090
