spring.application.name=cases-mt

# Datasource / JPA / Flyway / JWT are owned by application.yml + application-<env>.yml.
# Keep this file free of secrets; values come from env / profile YAML.
spring.datasource.url=${DB_URL:}
spring.datasource.username=${DB_USER:}
spring.datasource.password=${DB_PASSWORD:}
