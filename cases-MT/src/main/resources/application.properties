spring.application.name=cases-mt

# Default datasource — PostgreSQL (profile `dev` from application.yml).
# Team standard: shared RDS database `postgres` with schema isolation via currentSchema=cases_mt.
# Optional offline H2: mvnw spring-boot:run -Dspring-boot.run.profiles=h2
spring.datasource.url=jdbc:postgresql://insurance-db.cpmmyssa6fll.ap-south-1.rds.amazonaws.com:5432/postgres?currentSchema=cases_mt
spring.datasource.username=postgres
spring.datasource.password=postgres1234
spring.datasource.driver-class-name=org.postgresql.Driver
