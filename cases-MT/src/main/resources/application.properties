spring.application.name=cases-mt

# Datasource credentials MUST come from Athena-Cases/.env (DB_URL / DB_USER / DB_PASSWORD).
# Do not put secrets here. application-dev.yml binds those env vars for the default `dev` profile.
# Schema is controlled only by DB_URL (e.g. optional ?currentSchema=cases_mt) — keep it
# consistent with the schema you inspect in the DB client.
