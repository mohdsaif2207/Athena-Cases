package com.athena.cases.common.enums;

/**
 * Canonical case-type codes stored on {@code cases.case_type}.
 * Display labels belong in UI / lookup — not here.
 */
public enum CaseTypeCode {
    BILLING_DEPARTMENT_REQUEST,
    DBM_WORK_ORDER_REQUEST,
    EXRT_REQUEST,
    COVERAGE_AMOUNT_REQUEST,
    REPORT_REQUEST,
    PROJECT_TRACKER_REQUEST
}
